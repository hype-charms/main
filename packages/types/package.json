{
  "name": "@hype-charms/types",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc -w --outdir ./dist",
    "playground:dev": "tsc -w --outdir dist ",
    "prebuild": "tsc --outdir ./dist",
    "build": "NODE_ENV=production webpack"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "babel-loader": "^9.1.2",
    "stripe": "^11.13.0"
  }
}